#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/reset.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <&bootloader &kp E &kp F>;
            sensor-bindings = <&inc_dec_kp A B>, <&inc_dec_kp C D>;
        };
    };
};